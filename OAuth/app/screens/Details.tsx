import { View, Text } from 'react-native';
import React from 'react';

const Details = () => {
    return (
        <View>
            <Text>Just another page...</Text>
        </View>
    );
};

export default Details;